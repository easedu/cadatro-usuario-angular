import { FilterCpfPipe } from './filter-cpf.pipe';

describe('FilterCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
